{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "eslint": "^9.3.0",
    "lerna": "^8.1.3"
  },
  "scripts": {
    "start:frontend": "lerna run serve --scope frontend",
    "start:backend": "lerna run start --scope backend"
  }
}
